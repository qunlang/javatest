package binli;

/**
 * Created by HOME_CD_COM on 2017/6/3.
 */
public class binli {

/*    int Number = 10000;
    int[] Num11= new int[11];
    int[] Numbernum = new int[Number];
    int ij = 0;

    for(int ij = 0 ; i < Number; ij++){
        Numbernum[ij]=(int)(Math.random()*Number);
    }

    //长度为11

    for(int ij = 0; i < 11 ; ij++){

        Num11[ij]=Numbernum[ij];
    }
        Arrays.sort(Num11);

    for(int ij= 0; ij<Number; ij++){
        Num11[10]=Numbernum[i];
        Arrays.sort(Num11);

    }
    for(int ij = 0; ij < 10 ; ij++) {
        System.out.print("Num11 "+Num11[i]+" ");
    }
*/
}
